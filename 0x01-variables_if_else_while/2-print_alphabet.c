#include <stdio.h>
/**
 * main - lowercase 
 *
 * Return: always 0
 */
int main(void)
{
	char lower[26] = "abcdefghijklmnopqrstuvwxyz";
	int i;

	for (i = 0; i < 26; i++)
		putcher(lower[i]);
	puthcer('\n');
	return (0);
}
